- keywords @post.relative_query_without_self

.page-header
  %h1= title @post.title

  %ul.list-inline.text-muted
    %li= l @post.published_at.to_date, format: :long
    - if signed_in?
      %li= link_to 'edit', edit_post_path(@post)
      %li= link_to 'delete', post_path(@post), method: :delete, data: { confirm: "Are you sure?" }
!~ @post.content

- @materials.each do |m|
  = render "web/shared/search_result/#{m.type}", object: m

= render 'web/shared/disqus'
